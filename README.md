# 项目说明

## 创建步骤

* 在 github 上创建 diary 仓库:
    * 添加 README.md;
    * 按照 Python 模板配置 `.gitignore`  文件.
* 本地执行 `git clone git@github.com:[username]/diary.git` 下载仓库;
* 切换到本地仓库目录,依次执行如下命pixi

```bash
pixi init
pixi add sphinx-autobuild
```





[diary](https://github.com/gaosiyan/diary)
